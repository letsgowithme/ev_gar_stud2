{% extends 'base.html.twig' %}

{% block title %}Option{% endblock %}

{% block body %}
    <h1>Option</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ option.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_option_index') }}">Vers la liste</a>

    <a href="{{ path('app_option_edit', {'id': option.id}) }}">Modifier</a>

    {{ include('option/_delete_form.html.twig') }}
{% endblock %}
